import type { Metadata, Viewport } from 'next'

export const metadata: Metadata = {
  title: 'Sanity Studio',
  description: 'Content management for PitchStartup Hub',
}

export const viewport: Viewport = {
  themeColor: '#000000',
}
